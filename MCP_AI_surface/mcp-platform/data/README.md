# Sample data for filesystem connector demo
This directory is served by the **filesystem-local** MCP connector.

Try:

```bash
mcp-cli call-tool read_file.filesystem-local --input '{"path": "README.md"}'
```
